'use strict'
/*
// task 1
let str = prompt('Введіть текст')
let counter = 0;
for (let i = 0; i < str.length; i++) {
    if(str.charAt(i)===' ') counter++;
}
console.log(counter);
*/

/*
// task 2
let str = prompt('Введіть email');
for (let i = 0; i < str.length; i++) {
    if(str.indexOf('@') === 0 || str.indexOf('@') === str.length - 1 || str.indexOf('@') === -1){
        console.error("Error")
    }
}
*/

/*
// task 3
let str = prompt('Введіть текст')
let start = 0;
let counter = 0;
while (true) {
    let position = str.indexOf('html', start);
    if (position === -1) break;
    counter++;
    start = position + 1;
}
console.log(`Вираз "html" ----->> ${counter}`);
*/

/*
//task4
let str = prompt('Введіть текст');
let flag = str.indexOf('w');
console.log(str.substring(flag));
*/
