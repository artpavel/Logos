// task 1
/*let n = parseInt(prompt('Введіть число'));
let n = 15;
 if (n < 0) {
     document.write(`<h2>Введіть натуральне число</h2>`);
 } else if (!isNaN(n)) {
    let resultat = 1;
    for (let i = 1; i <= n; i++) {
        resultat *= i;
     }
    document.write(`<h2>Дорівнює ${resultat}</h2>`);
 } else {
    document.write(`<h2>Введіть натуральне число</h2>`);
 }*/

// task 2
// for (let count = 1000; count <= 9999; count += 3) {
//     document.write(`<h2>Дорівнює ${count}</h2>`);
// }

// task 3
// for (let a = 1, b = 1; a <= 55; a++, b += 2) {
//     document.write(`<h2>Дорівнює ${b} => елемент в прогресії ${a}</h2>`);
// }

// task 4
// for (let count = 90; count >= 0; count -= 5) {
//     document.write(`<h2>Рахуй ${count}</h2>`);
// }

// task 5
// for (let a = 1, b = 2; a <= 20; a++, b *= 2) {
//     document.write(`<h2>Дорівнює ${b} => порядок елемента  ${a}</h2>`);
// }

// task 6
// for (let a = 2; a < 10000; a = 2 * a - 1) {
//     document.write(`<h2>Дорівнює ${a}`);
// }

// task 7
/*for (let a = -166; a < 100; a = 2 * a + 200) {
    if (a > -100) {
        document.write(`<h2>Дорівнює ${a}`);
    }
}*/

// task 8
// let a = +prompt('Введіть число');
// let b = +prompt('Введіть степінь числа');
// let result = 1;
// if (b > 0 || b < 0) {
//     for (let i = 1; i <= Math.abs(b); i++) {
//         result = result * a;
//     }
//     if (b < 0) {
//         result = 1 / result;
//         document.write(`<h2>Дорівнює ${result}`);
//     } else {
//         document.write(`<h2>Дорівнює ${result}`);
//     }
// } else {
//     result = 1;
//     document.write(`<h2>Дорівнює ${result}`);
// }
